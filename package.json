{
  "name": "ios-pitfalls",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "npm run sync && npm run validate"
  }
}
